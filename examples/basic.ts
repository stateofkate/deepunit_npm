// base logic
export function testFunction(arg: number): number {
  let test1 = 6;
  if (arg > 5) {
    return test1 - arg;
  } else {
    return test1 + arg;
  }
}

// basic imports test
export function canReadFile(arg: number): number {
  let test1 = 6;
  if (arg > 5) {
    return test1 - arg;
  } else {
    return test1 + arg;
  }
}

// basic global variable

// base logic and import test
