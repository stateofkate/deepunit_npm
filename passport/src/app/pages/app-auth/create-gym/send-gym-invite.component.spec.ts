/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SendGymInviteComponent } from './send-gym-invite.component';

describe('CreateGymComponent', () => {
  let component: SendGymInviteComponent;
  let fixture: ComponentFixture<SendGymInviteComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SendGymInviteComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(SendGymInviteComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
