/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GymSetupComponent } from './gym-setup.component';

describe('GymSetupComponent', () => {
  let component: GymSetupComponent;
  let fixture: ComponentFixture<GymSetupComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GymSetupComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(GymSetupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
