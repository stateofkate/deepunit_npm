/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ListGymInvitesComponent } from './list-gym-invites.component';

describe('ListGymsComponent', () => {
  let component: ListGymInvitesComponent;
  let fixture: ComponentFixture<ListGymInvitesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ListGymInvitesComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ListGymInvitesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
