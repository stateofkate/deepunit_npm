/*
import { TestBed } from '@angular/core/testing';

import { GymAuthGuardGuard } from './gym-auth-guard.guard';

describe('GymAuthGuardGuard', () => {
  let guard: GymAuthGuardGuard;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    guard = TestBed.inject(GymAuthGuardGuard);
  });

  it('should be created', () => {
    expect(guard).toBeTruthy();
  });
});
*/
