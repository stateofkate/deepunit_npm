/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GymSignupComponent } from './gym-signup.component';

describe('GymSignupComponent', () => {
  let component: GymSignupComponent;
  let fixture: ComponentFixture<GymSignupComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GymSignupComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(GymSignupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
